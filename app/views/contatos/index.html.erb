<h1>Contatos</h1>

<table class="table">
  <thead>
  <tr>
    <th>Id</th>
    <th>Nome</th>
    <th>Email</th>
    <th>Mensagem</th>
    <th>Data</th>
    <th></th>
  </tr>
  </thead>

  <tbody>
  <% for contato in @contatos %>
      <tr>
        <td><%= contato.id %></td>
        <td><%= contato.nome %></td>
        <td><%= mail_to contato.email %></td>
        <td><%= contato.mensagem %></td>
        <td><%= l contato.created_at, format: :short %></td>
        <td><%= link_to 'abrir', view_contato_path(contato), class: 'btn btn-info btn-xs' %></td>
      </tr>
  <% end %>
  </tbody>
</table>